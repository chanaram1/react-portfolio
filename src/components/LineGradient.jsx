import React from 'react'

const LineGradient = ({width="w-full"}) => { //default wideth is full
  return (
    <div className={`h-0.5 ${width} bg-gradient-rainblue`} />
      
  )
}

export default LineGradient